# ProductManager v2.1

The included `README.md` previously shipped with examples that no longer match the current implementation. Please use `corrected_readme.md` for runnable examples and accurate API usage.

Summary of issues: outdated function names and parameter names, invalid example values (negative prices/stock), and references to removed features such as password-protected exports. See `defects.txt` for a full list of mismatches and traces.

## Corrected Quick Start

See `corrected_readme.md` for verified examples that run with the bundled `product_manager.py` implementation.
